@model BrandDeatailModel
@{
    ViewData["Title"] = "Info";
    Layout = "_HomeLayout";
}
<section class="home-section">
    <div class="card-body">
        <form asp-action="BrandDeatail" method="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="BussneName" class="control-label"></label>
                <input asp-for="BussneName" placeholder="Bussness Name" class="form-control card" />
                <span asp-validation-for="BussneName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdentityDocument" class="control-label"></label>
                <div class="custom-file">

                    <input type="file" multiple asp-for="IdentityDocument" class="form-control custom-file-input" />
                    <label asp-for="IdentityDocument" class="custom-file-label">Chose-File...</label>
                    <span asp-validation-for="IdentityDocument" class="text-danger">@ViewBag.file</span>
                </div>

            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</section>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
