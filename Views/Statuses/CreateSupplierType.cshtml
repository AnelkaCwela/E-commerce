@model OurShop.Models.DataModel.SupplierTypeModel

@{
    ViewData["Title"] = "Create CreateType";
    Layout = "_HomeLayout";
}

<section class="home-section">
    <h4>Create CreateSupplier Type</h4>
    <hr />
    <div class="alert-primary font-weight-bolder">
        @ViewBag.Success
    </div>
    <div class="alert-danger font-weight-bolder">
        @ViewBag.USuccess
    </div>
    <a class="text-dark" asp-area="" asp-controller="Manage" asp-action="Index">Back</a>
    <div class="row">
        <div class="col-md-4">
            <form asp-action="CreateSupplierType">

                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="SupplierType" class="control-label"></label>
                    <input asp-for="SupplierType" class="form-control" />
                    <span asp-validation-for="SupplierType" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <button type="submit" style="width:auto" value="Create" class="btn btn-primary">Create</button>
                </div>

            </form>
        </div>
    </div>
</section>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
