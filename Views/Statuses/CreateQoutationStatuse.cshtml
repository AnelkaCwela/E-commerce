@model OurShop.Models.DataModel.QoutationStatuseModel

@{
    ViewData["Title"] = "Create Qoutation Statuse";
    Layout = "_HomeLayout";
}
<section class="home-section">
    <h4>Create Qoutation Statuse</h4>
    <hr />
    <div class="alert-primary font-weight-bolder">
        @ViewBag.Success
    </div>
    <div class="alert-danger font-weight-bolder">
        @ViewBag.USuccess
    </div>
    <a class="text-dark" asp-area="" asp-controller="ManageIteam" asp-action="Index">Back</a>
    <div class="row">
        <div class="col-md-4">
            <form asp-action="CreateQoutationStatuse">

                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="QoutationStatuse" class="control-label"></label>
                    <input asp-for="QoutationStatuse" class="form-control" />
                    <span asp-validation-for="QoutationStatuse" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <button type="submit" style="width:auto" value="Create" class="btn btn-primary">Create Qoutation Statuse</button>
                </div>

            </form>
        </div>
    </div>
</section>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
